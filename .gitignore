# Reference implementation — internal-only, NEVER ship to aws-samples
_reference/

# Local-only artifacts (study notes, never shipped)
ASSESSMENT.md
DELTA.md
_inventory_backend.md
_inventory_frontend.md
_inventory_tabs.md
_probe_results/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# Node
node_modules/
dist/
build/
.next/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Env
.env
.env.local
*.local

# Logs
*.log

# Customer-edited config (committed only as `config.example.yaml`)
config.yaml

# Claude Code local settings (contains caller account ID + tool allowlists)
.claude/settings.local.json

# Playwright artifacts + screenshots (CI-generated, do not commit)
test-results/
playwright-report/

# Temporary handoff docs from one-off agent comms
HANDOFF-internal-bedrock-lens.md

# Internal-only spec used to drive the original port (not customer-facing)
bedrock-lens-customer-facing-spec.md

# CDK build output (regenerated by `cdk synth`)
infra/cdk.out/
infra/cdk.context.json
infra/node_modules/

# Per-clone deploy stack-name pin (auto-generated by deploy.sh)
.deploy-stack-name

# Playwright artifacts (generated by `npx playwright test`)
frontend/tests/screenshots/
frontend/test-results/
frontend/playwright-report/

# PowerPoint lock files
~$*.pptx
~$*.docx

# PPTX backup snapshots (kept locally only)
_pptx-backups/

# MCP venv + caches
mcp/.venv/
mcp/**/__pycache__/
mcp/*.egg-info/
mcp/src/*.egg-info/
mcp/build/

# Local-only artifacts and internal docs (never shipped)
.scan-artifacts/
docs/
