fastapi==0.115.6
uvicorn[standard]==0.32.1
asyncpg==0.30.0
boto3>=1.43.0
pyyaml==6.0.2
# JWT verification for Cognito ID tokens (only loaded when AUTH_ENABLED=true,
# but we install unconditionally so the prod image is auth-ready).
python-jose[cryptography]==3.5.0  # 3.3.0 was vulnerable to GHSA-6c5p-j8vq-pqhj + GHSA-cjwg-qfpm-7377
# Mangum bridges ASGI (FastAPI) to AWS Lambda's invocation event shape.
# Used only by the Lambda container's CMD; ignored when running locally
# under uvicorn or in Fargate. Adding it to requirements means one image
# works in all three modes.
mangum==0.19.0
# pymemcache for ElastiCache Memcached client (production cache backend).
# Not strictly required for local dev (in-memory dict fallback works).
pymemcache==4.0.0
