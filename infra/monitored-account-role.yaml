AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Bedrock Ops Lens — read-only IAM role for cross-account metric ingestion.
  Deploy this in EVERY account whose Bedrock usage you want surfaced in the
  central dashboard. Supports self-managed StackSets without Organizations,
  service-managed StackSets, or deployment by each account's administrator.

Parameters:
  CentralAccountId:
    Type: String
    Description: >
      AWS account ID where Bedrock Ops Lens is deployed (the "central"
      observability account that runs the ingester).
    AllowedPattern: '^[0-9]{12}$'

  CentralIngesterRoleArn:
    Type: String
    Default: ''
    AllowedPattern: '^$|^arn:[a-z0-9-]+:iam::[0-9]{12}:role/.+$'
    Description: >
      IAM role ARN used by the central ingester Lambda. setup-pipeline.sh
      supplies this to restrict trust to that role. Leaving it empty retains
      central-account trust for backwards compatibility; principals in that
      account must also have permission to assume this reader role.

  RoleName:
    Type: String
    Default: BedrockOpsLensReader
    AllowedPattern: '^[A-Za-z0-9+=,.@_-]{1,64}$'
    Description: >
      IAM role name. Must match ReaderRoleName in the central stack, which
      configures the ingester and its assume-role permission.

  ExternalId:
    Type: String
    Default: ''
    Description: >
      Optional external ID for the trust policy (defense-in-depth against the
      "confused deputy" problem). If set, the central ingester must pass the
      same value when calling sts:AssumeRole.

Conditions:
  HasExternalId: !Not [!Equals [!Ref ExternalId, '']]
  HasIngesterRole: !Not [!Equals [!Ref CentralIngesterRoleArn, '']]

Resources:
  # Use a customer-managed policy instead of an inline policy (cfn-guard IAM_NO_INLINE_POLICY_CHECK).
  BedrockOpsLensReaderRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Ref RoleName
      Description: 'Read-only role used by Bedrock Ops Lens to ingest CloudWatch metrics and Service Quotas.'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Sid: TrustCentralIngester
            Effect: Allow
            Principal:
              AWS: !If
                - HasIngesterRole
                - !Ref CentralIngesterRoleArn
                - !Sub 'arn:${AWS::Partition}:iam::${CentralAccountId}:root'
            Action: sts:AssumeRole
            Condition:
              !If
                - HasExternalId
                - StringEquals:
                    sts:ExternalId: !Ref ExternalId
                - !Ref AWS::NoValue
      ManagedPolicyArns:
        - !Ref BedrockOpsLensReadPolicy

  # Managed policy holding the read-only permissions previously inlined on the role.
  BedrockOpsLensReadPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      ManagedPolicyName: !Sub '${RoleName}-Read'
      Description: 'Read-only permissions for Bedrock Ops Lens cross-account ingestion.'
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          # CloudWatch — list and pull AWS/Bedrock metrics.
          - Sid: CloudWatchRead
            Effect: Allow
            Action:
              - cloudwatch:ListMetrics
              - cloudwatch:GetMetricData
              - cloudwatch:GetMetricStatistics
            Resource: '*'
          # Service Quotas — applied + default values.
          - Sid: ServiceQuotasRead
            Effect: Allow
            Action:
              - servicequotas:GetServiceQuota
              - servicequotas:GetAWSDefaultServiceQuota
              - servicequotas:ListServiceQuotas
              - servicequotas:ListAWSDefaultServiceQuotas
              - servicequotas:ListServices
            Resource: '*'
          # Bedrock — read-only, only for the invocation-logging-config check
          # (so the dashboard can tell the operator whether logging is on).
          - Sid: BedrockReadOnly
            Effect: Allow
            Action:
              - bedrock:GetModelInvocationLoggingConfiguration
              - bedrock:ListFoundationModels
              - bedrock:GetFoundationModel
              - bedrock:ListInferenceProfiles
            Resource: '*'
          # Account name — lets the dashboard label this account with its
          # friendly name instead of the bare 12-digit ID. Self-scoped
          # read-only; works without AWS Organizations. See dim_account.
          - Sid: AccountNameRead
            Effect: Allow
            Action:
              - account:GetAccountInformation
            Resource: '*'

Outputs:
  RoleArn:
    Description: ARN of the cross-account reader role.
    Value: !GetAtt BedrockOpsLensReaderRole.Arn
    Export:
      Name: !Sub '${AWS::StackName}-RoleArn'
